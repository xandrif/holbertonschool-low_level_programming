#include "holberton.h"

/**
 * more_numbers - 012345678910111121314 newline
 * @void: does not take any parameters
(*
* Description: Longer description of the function)?
(* section header: Section description)*
* Return: 0 success
*/

void more_numbers(void)
{
	int i = 0, j;

	while (i <= 10)
	{
		j = 0;
		while (j <= 14)
		{
			_putchar((j % 10) + '0');
			if (j >= 10 && j <= 14)
			{
				_putchar((j / 10) + '0');
			}
			j++;
		}
		_putchar('\n');
		i++;
	}
}
