#include "holberton.h"

/**
 *  _isdigit - returns 1 if something is uppercase
 * @c: an integer
(*
 * Description: Longer description of the function)?
(* section header: Section description)*
* Return: 0
*/

int _isdigit(int c)
{
	if (c >= 48 && c <= 57)
	{
		return (1);
	}

	else
	{
		return (0);
	}
}
