#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{

	char c;
	for (c = 'a' ; c <= 'z' ;)
	{
		if (c == 'e' || c == 'q')
		{
			c++;
		}
		putchar (c);
		c++;
	}
	return (0);
}
