#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int m;

	for (m = 48; m < 58; m++)
	{
		putchar(m);
	}
	for (m = 97; m < 103; m++)
	{
		putchar(m);
	}
	putchar(10);
	return (0);
}
